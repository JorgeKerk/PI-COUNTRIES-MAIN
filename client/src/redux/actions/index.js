import { 
    getAllCountries, 
    getCountryByName,
    setCurrentCountries
} from './countries'

import { 
    createActivity, 
    getAllActivities, 
    setFilterByActivity,
} from './activities'

import {
    setCurrentPage,
    setFilterByContinents,
    setNamCountries,
    setOrder,
    setNewActivity,
    clearNewActivity,
    setError
} from './config'

export { 
    getAllCountries, 
    getCountryByName,
    setCurrentCountries,
    createActivity,
    getAllActivities,
    setCurrentPage,
    setNamCountries,
    setFilterByActivity,
    setFilterByContinents,
    setOrder,
    setNewActivity,
    clearNewActivity,
    setError
}